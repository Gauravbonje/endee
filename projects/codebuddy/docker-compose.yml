version: "3.9"

services:
  endee:
    image: ghcr.io/endee-io/endee:latest
    container_name: endee-db
    ports:
      - "8080:8080"
    volumes:
      - endee_data:/data
    restart: always

volumes:
  endee_data: